services:
  app:
    build: .
    ports:
      - "58085:80"
    environment:
      - MYSQL_ROOT_PASSWORD=secret
    links:
      - mysql

  mysql:
    image: yobasystems/alpine-mariadb
    environment:
      - MYSQL_DATABASE=phpservermonitor
      - MYSQL_ROOT_PASSWORD=secret

  #adminer:
  #  image: adminer
  #  ports:
  #    - "8080:8080"