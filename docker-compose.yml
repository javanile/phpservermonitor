services:
  phpservermonitor:
    build: .
    ports:
      - "58085:80"
    environment:
      - MYSQL_ROOT_PASSWORD=secret
    links:
      - mysql

  mysql:
    image: mysql:8.0.35
    environment:
      - MYSQL_DATABASE=phpservermonitor
      - MYSQL_ROOT_PASSWORD=secret
